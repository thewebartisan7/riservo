# Handoff

**Session**: 0 — Bootstrap  
**Date**: 2026-04-12  
**Status**: Ready for Session 1

---

## What Was Done

Bootstrap session — no code written. Documentation reviewed, made consistent, and data model gaps filled.

### CLAUDE.md approach
Two separate files, kept as-is:
- **Root `CLAUDE.md`**: auto-generated by Laravel Boost — Laravel conventions, Pint, Pest, Artisan, PHP style rules
- **`.claude/CLAUDE.md`**: project-specific guide — session workflow, critical rules, conventions

Both are loaded automatically by Claude Code. No cross-references needed. Removed duplicated content (run commands, test commands) from `.claude/CLAUDE.md`.

### Documentation fixes applied
- **URL prefix**: standardized to `/{slug}` everywhere (removed `/b/` prefix) → D-013
- **Roles**: three MVP roles — `admin`, `collaborator` (business-scoped), `customer` (separate auth context) → D-014
- **Slot interval**: moved from business-level to per-service (`slot_interval_minutes` on Service model) → D-015
- **Cancellation window**: clarified as customer-only; admins can always cancel → D-016
- **Business hours**: new `business_hours` table, separate from collaborator `AvailabilityRule` → D-017
- **Exception date ranges**: `AvailabilityException` uses `start_date` + `end_date` instead of single `date` → D-018
- **Reminder config**: `reminder_hours` JSON array on Business → D-019
- **Price on request**: nullable decimal `price` on Service — null means "on request" → D-020
- **Service slug**: added `slug` field to Service (required for `/{slug}/{service-slug}` URLs)
- **Business contact fields**: added `phone`, `email`, `address` to Business
- **Tech stack**: SPEC §4 now explicitly says Laravel 13, PHP 8.3, TypeScript
- **Collaborator avatar upload**: moved from Session 5 to Session 9
- **Embed functionality**: moved from Session 7 to Session 9
- **Completed status job**: added scheduled job to Session 10
- **Session 7 email**: noted as placeholder — real templates built in Session 10
- **SPEC numbering**: fixed §7.5/§7.6 gap
- **SESSION-PROMPT.md reference**: removed (template lives outside the project)

### Open proposals for future agents
- **P-001**: Assignment strategy — should it be configurable (first_available vs round_robin) or always first_available in MVP? Session 3 agent to evaluate.
- **P-002**: React i18n approach — how to handle `__()` translations in React/Inertia. Session 4 agent to research.

---

## Current Project State

- Fresh Laravel 13 installation: **provided by developer** (no starter kit)
- Database: not configured
- Frontend: not installed
- Tests: none

---

## What Session 1 Needs to Know

Session 1 starts from a **fresh Laravel 13 installation with no starter kit**.

Tasks for Session 1:
- Configure `.env` for SQLite local development
- Set up directory structure conventions
- Configure Pint, Larastan, Pest
- Commit a clean baseline

---

## Open Questions / Deferred Items

- VenaUI (`vena-ui`) npm package: confirm it is published and available before Session 4
- Confirm Blueprint (`laravel-shift/blueprint`) is compatible with Laravel 13 before Session 2
- Hostpoint deployment details to be confirmed before Session 10 (notifications) or whenever file storage is first needed
