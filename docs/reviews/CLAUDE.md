# Reviews Guidance

This directory holds the **active review round only**. It is empty between rounds.

- Round 1 (REVIEW-1 + ROADMAP-REVIEW-1) closed on 2026-04-16 and was archived to `docs/archive/reviews/`.
- Round 2 (REVIEW-2 + ROADMAP-REVIEW-2, if remediation is needed) is pending and will land here when it does.

Rules:

- Do not bulk-read this directory.
- Read the specific review file referenced by the user or clearly required by the current task.
- For the closed Round 1 (REVIEW-1 findings + ROADMAP-REVIEW-1 remediation roadmap), look under `docs/archive/reviews/`.
- For general project context, prefer active docs first: `docs/README.md`, `docs/HANDOFF.md`, `docs/ROADMAP.md`, `docs/SPEC.md`, and `docs/DECISIONS.md`.

On archive of a round:

- Files move to `docs/archive/reviews/`.
- The review file keeps its name (`REVIEW-{N}.md`).
- The roadmap file is numbered on archive: `ROADMAP-REVIEW.md` → `ROADMAP-REVIEW-{N}.md` (first round renamed on close; subsequent rounds carry the number from birth).
