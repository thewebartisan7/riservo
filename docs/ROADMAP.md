# riservo.ch — Customer-to-Professional Payments Roadmap

> Status: draft (pending a further review round before Session 1 opens).
> Scope: End-to-end online payment flow where the customer pays the professional at booking time. Stripe Connect Express, TWINT-first, zero riservo commission. Separate from MVPC-3 (riservo's own SaaS billing on the Business model).
> Format: WHAT only. The HOW is decided per-session by the implementing agent in a dedicated plan document.
> Each session is a focused, reviewable unit handed to a single agent. Sessions run strictly sequentially; prerequisites are listed per-session below.

---

## Overview

The `payment_mode` enum on `Business` has existed as a data field since the early schema (values `offline`, `online`, `customer_choice`) but no flow backs the `online` or `customer_choice` values. SPEC §12 currently lists Stripe Connect and online payments as v2; this roadmap promotes them to a real near-term deliverable for the Swiss market, where TWINT availability is a competitive differentiator and customers already expect to pay at booking for certain verticals (beauty, wellness, coaching).

**Revenue model is unchanged**: riservo takes **zero commission** on customer-to-professional payments. Riservo's revenue remains purely the SaaS subscription (MVPC-3, Cashier on `Business`). Stripe processing fees are paid by the professional to Stripe directly via their connected account. This roadmap is a product feature, not a monetisation layer.

The new sequence is six sessions (Session 2 split post-review into a happy-path half and a failure-branching half), each with a clear deliverable and a single owning agent:

| # | Session | Prerequisites | Outcome |
|---|---------|---------------|---------|
| 1 | Stripe Connect Express Onboarding | MVPC-3 | Business connects a Stripe Express account via Stripe-hosted KYC; verification status persisted; `payment_mode` UI still locked to `offline` only |
| 2a | Payment at Booking — Happy Path | Session 1 | Public booking flow creates `pending + awaiting_payment` booking; hosted Stripe Checkout (card + TWINT) opens; `checkout.session.completed` webhook + synchronous success-page retrieve promote the booking |
| 2b | Payment at Booking — Failure Branching + Admin Surface | Session 2a | Expired / failed / abandoned Checkout handled per `payment_mode_at_creation`; expiry reaper with defense-in-depth (pre-flight + grace buffer + late-webhook refund); admin booking-detail payment panel; `unpaid` badge and filters |
| 3 | Refunds (Customer Cancel, Admin Manual, Business Cancel) + Disputes | Session 2b | Refund flow wired three ways; automatic refunds on in-window customer cancels and business-side cancels; admin-initiated manual refund; dispute webhook surfaces a Pending Action |
| 4 | Payout Surface + Connected Account Health | Session 3 | Dashboard section showing connected-account status, next payout ETA, recent payouts, deep-link to Stripe Express dashboard |
| 5 | `payment_mode` Toggle Activation + UI Polish | Sessions 1, 2a, 2b, 3, 4 | Lift the hide-the-options ban in Settings → Booking; `online` and `customer_choice` become user-facing; settle the copy, empty states, and error banners; gate non-CH connected accounts out of online modes |

Alternatives considered briefly before Session 1 is planned:

- **Mollie Connect** — Dutch PSP with native TWINT and a simpler marketplace abstraction. Rejected because we would introduce a second billing SDK alongside Cashier/Stripe (MVPC-3), splitting webhook plumbing, dashboard UX, and operational knowledge across two providers.
- **Datatrans** — Swiss-native PSP with first-class TWINT. Rejected because Datatrans does not provide a marketplace / connected-account abstraction — we would build account onboarding, KYC state machines, and payout orchestration from scratch, which is explicitly not where riservo's engineering effort belongs.
- **Stripe Connect Express** — picked. TWINT is supported natively on CH connected accounts; Express onboarding is Stripe-hosted (no PCI burden and no KYC UX to build); Stripe Tax integrates cleanly with Connect; Cashier already establishes Stripe as the SaaS stack, so we reuse SDK, webhook signature validation, idempotency patterns, and operational know-how. Standard is rejected as it hides the riservo brand poorly during onboarding; Custom is rejected because it moves the PCI + KYC burden onto us.

Sessions 1, 2a, 2b, 3, and 4 keep the `online` and `customer_choice` options hidden from the Settings → Booking UI. Session 5 lifts the hide, which is why it ships last.

The SaaS-side billing work (MVPC-3, shipped) is independent of this roadmap. Terminology is chosen to keep the two clearly separate: MVPC-3 uses "subscription", "billing", "trial"; this roadmap uses "payment", "charge", "refund", "payout", "connected account".

---

## Cross-cutting decisions locked in this roadmap

These are the decisions the implementing agents must not re-evaluate. Each must be recorded with a fresh decision ID in `docs/decisions/DECISIONS-PAYMENTS.md`. The next available `D-NNN` is whatever `docs/HANDOFF.md` states at session time (the authoritative counter lives there, not in this roadmap body).

### Commercial model and PSP selection

1. **Zero commission**. Riservo takes no cut of any customer-to-professional payment. The full service price, minus Stripe processing fees, lands on the connected account. Cashier subscription revenue (MVPC-3) is riservo's only income stream. Locked.
2. **Stripe Connect Express**. Not Standard, not Custom. Rationale: Stripe-hosted KYC, TWINT on CH accounts out of the box, marketplace abstraction without PCI burden, same SDK as MVPC-3. Locked.
3. **TWINT is mandatory for CH-located businesses**. TWINT is enabled by default (not opt-in) on every Stripe Checkout session for a business whose connected account country is CH. Rationale: over 5M Swiss users, the dominant wallet in the target market; opt-in puts the product behind competitors by default. Locked.
4. **Hosted Stripe Checkout, not embedded Elements**. Rationale: less PCI surface, TWINT + card out of the box, 3DS / SCA handled automatically by the hosted page, mobile behaviour is Stripe's problem. Locked.
5. **Direct charges with `Stripe-Account` header, not destination charges**. Combined with locked decision #1 (zero commission) and #22 (one connected account per Business), the professional is the merchant of record. Direct charges put the Stripe Customer, the Charge, the Invoice, and the receipt email on the connected account, branded as the professional (not "riservo"). Destination charges with `application_fee_amount = 0` would technically work but are non-idiomatic and put riservo as the platform-of-record on every charge for no benefit. Locked.

### When and what to charge

6. **Charge at booking confirmation (auto-capture)**. Customer pays immediately on "Confirm booking" — not auth-and-capture at appointment time, not customer-choice at the checkout step. Rationale: simplest mental model for the customer, no card expiry between auth and capture, matches the offline cash-at-door flow timing-wise. Auth-and-capture is a v2 consideration once professionals request it. Locked.
7. **Full charge upfront, not a deposit**. The full `Service.price` is charged at booking. Deposit-plus-balance is deferred — it introduces a second charge flow and an offline reconciliation surface we do not want in this roadmap. Locked.
8. **`Service.price = null` ("on request") cannot be booked online**. Services without a set price force the `offline` flow irrespective of the Business's `payment_mode`. The public booking UI gates the online path on `price !== null && price > 0`. A service with `price = 0` is also treated as offline — there is nothing to charge. Locked.
9. **Invoice generation is Stripe-side**. The connected account's Stripe dashboard surfaces PDF invoices automatically; riservo does not generate a parallel invoice document. The Business can link to Stripe from the riservo dashboard (Session 4). A riservo-native invoicing engine is a v2 product consideration, not an MVP requirement for this roadmap. Locked.
10. **VAT / tax handled on the connected account via Stripe Tax**. The professional configures Stripe Tax settings on their own Stripe account; riservo does not compute, store, or display tax on bookings. Analogous reasoning to D-094 for the SaaS side: tax is the taxable entity's problem, not the platform's. Locked.
11. **Stripe Tax not configured = warning banner, not hard block**. Many small Swiss businesses have never enabled Stripe Tax. Riservo's onboarding (Session 1) does NOT make Stripe Tax setup mandatory — that would block legitimate professionals from enabling online payments. Instead, the Connected Account dashboard page shows a persistent "Stripe Tax not yet configured — your customers' receipts will not show VAT. [Configure in Stripe →]" warning when the connected account's tax settings status is not `active`, deep-linking to the Stripe Tax setup page. Soft enforcement; the professional decides. The same warning surfaces in Session 4's payout page health strip. Locked.

### Reservation, failure, and race semantics

12. **Slot-hold pattern = reserve-then-pay**. On "Confirm booking" with online payment the Booking row is created immediately as `pending` with `payment_status = awaiting_payment`; the slot is locked by the existing Postgres GIST invariant (D-065, D-066). The customer is redirected to hosted Checkout. On `checkout.session.completed` the webhook promotes the booking to `confirmed` + `payment_status = paid`. The GIST constraint protects against double-booking during the Checkout window at no extra cost, which is the key reason we pick this pattern over "hold the slot only after payment succeeds". Locked.
13. **Checkout expiry = 90 minutes; reaper applies only to `payment_mode_at_creation = online`**. Stripe Checkout session is created with `expires_at = now + 90 minutes` (Stripe default is 24h; we override). 90 minutes accommodates TWINT's slower flow (app switch + authentication + occasional SMS) and slow card customers, while still freeing genuinely abandoned slots in a reasonable window. The bump from 60 to 90 minutes is a deliberate hedge against the reaper-vs-webhook race (decision #31): a legitimate TWINT completion at minute 61 must not be cancelled by the reaper. The reaper job (`bookings:expire-unpaid`) cancels stale `pending + awaiting_payment` bookings ONLY when the underlying booking's `payment_mode_at_creation` is `online` — for `customer_choice` the booking is promoted to `confirmed + unpaid` instead (decision #14), so it is never a candidate for the reaper. The booking row's `expires_at` column is set only for `online` mode; for `customer_choice` it is left null. The reaper's behaviour under race conditions is defined in decision #31. Locked.
14. **Failed/abandoned Checkout behaviour depends on `payment_mode_at_creation`**. The Business's `payment_mode` is captured on the booking row at creation time (new column `payment_mode_at_creation`, mirrors `Business.payment_mode` at booking time) so subsequent setting changes do not re-interpret in-flight bookings.
    - **`payment_mode_at_creation = online`**: failed Checkout (`payment_intent.payment_failed`), expired Checkout (`checkout.session.expired`), or customer abandonment via the cancel URL → booking cancelled, slot freed, payment_status terminal. Customer return page reads "Slot released — try again to rebook". This matches the Business's commercial intent ("require online payment").
    - **`payment_mode_at_creation = customer_choice` and the customer picked online at Checkout**: failed/expired/abandoned → booking promoted to `confirmed` with `payment_status = unpaid` (new value, see decision #28); slot stays held; the standard booking-confirmed notifications fire normally. Customer return page reads "Payment not completed — your booking is confirmed; pay at the appointment". The Business sees the booking with an "Unpaid" badge in the dashboard. Rationale: the customer affirmatively confirmed the slot before the redirect; the Business's `customer_choice` setting explicitly permits paying offline; the natural fallback for a failed-prepay attempt is "you can still pay on site".

    **Snapshot invariant — `payment_mode_at_creation` always mirrors the Business's `payment_mode` at booking creation, never the customer's Checkout-step choice.** The three `customer_choice` outcomes are:
    1. Customer picked "pay now" → Checkout succeeded → `confirmed + paid`, `payment_mode_at_creation = 'customer_choice'`.
    2. Customer picked "pay now" → Checkout failed / expired / abandoned → `confirmed + unpaid` (under auto-confirm) or `pending + unpaid` (under manual confirmation per decision #29), `payment_mode_at_creation = 'customer_choice'`.
    3. Customer picked "pay on site" upfront at the Checkout step → no Stripe Checkout session is created → `confirmed + not_applicable` (or `pending + not_applicable` under manual confirmation), `payment_mode_at_creation = 'customer_choice'` — **NOT `'offline'`**. The customer's Checkout-step choice decides whether a Checkout session is created; it does NOT rewrite the snapshot.
    The sole carve-out to the mirror rule is decision #30: `source = manual` and `source = google_calendar` bookings always write `payment_mode_at_creation = 'offline'` regardless of the Business's current mode. This preserves the analytics / filter-chip semantics: a `customer_choice + not_applicable` booking is "the customer chose to pay on site on a Business that offers the choice" (surfaces in dashboards under both the `customer_choice` grouping and the `offline` payment chip), whereas an `offline + not_applicable` booking is "the Business does not accept online payment at all". Locked.

### Refund policies

15. **Refund on customer cancel INSIDE the cancellation window = full automatic refund**. No business-level toggle in this roadmap; the cancellation window is already the business's stated policy (D-016), so honouring it fully is the only consistent stance. A per-business "no-refund" override is a v2 request if it materialises. Locked.
16. **Refund on customer cancel OUTSIDE the cancellation window = no automatic refund; business can issue manually**. The charge stands; the customer sees "Outside cancellation window — contact [Business] for a refund". The Business can refund manually from the booking detail page. Partial refunds are supported as a free-form amount (manually entered, not a preset percentage). Locked.
17. **Refund on business-side cancel = full automatic refund, always**. Rationale: trust. A customer whose appointment is cancelled by the provider must not also have to chase their money. Locked.
18. **Insufficient connected-account balance for refund**. Stripe handles this via its platform-wide debit-on-file default: if the connected account balance is insufficient, Stripe debits the account's attached bank. Riservo does not block the refund and does not surface the debit mechanics in the UI — the professional owns that relationship with Stripe. Surface the final refund outcome (succeeded / failed) via the webhook-driven booking state. Locked.
19. **Refund initiator permissions**. Admin can issue refunds from any booking. Staff (with a linked Provider) cannot issue refunds even for their own bookings — refund authority is an admin-only commercial decision. Locked.

### Connected-account lifecycle

20. **KYC failure after account creation → `payment_mode` remains / reverts to `offline`**. If the connected account is created but Stripe verification fails (document rejected, owner information missing, etc.), the Business's `payment_mode` is forced back to `offline` by the `account.updated` webhook handler. A persistent banner in the dashboard reads "Complete Stripe onboarding to enable online payments" with a deep-link back to Express onboarding. The verification status is stored on the connected-account row. Locked.
21. **Business disconnects Stripe → `payment_mode` reverts to `offline`; existing paid bookings stay valid**. Paid `pending` / `confirmed` bookings remain in place (money is already with the professional); no new `online` bookings can be created; `payment_mode` is forced to `offline` automatically. Re-connecting restores the option to switch back. The refund behaviour after disconnect (automatic attempts may fail; Pending Action surfaces them) is defined in decision #36. Locked.
22. **Multi-business user = one connected account per Business**. A user who administers two businesses must onboard each one independently; the connected account is a Business-level resource, not a User-level resource. Schema: `stripe_connected_accounts` table keyed by `business_id` with a unique constraint, not by `user_id`. Locked.

### Payouts, disputes, and operator surfaces

23. **Payouts use Stripe's automatic schedule default (daily rolling)**. No riservo-level payout controls in MVP. The Business can change their payout schedule directly in the Stripe Express dashboard if they want weekly or manual. Locked.
24. **Riservo surfaces payout status, not payout management**. Session 4's dashboard section shows: connected-account verification status, next scheduled payout ETA + amount, last three payouts (date + amount + status), and a deep-link "Manage payouts in Stripe". No payout initiation, no payout pause, no payout schedule change from riservo. Locked.
25. **Disputes / chargebacks are resolved via Stripe's UI; riservo surfaces awareness, not resolution**. The professional resolves disputes (evidence upload, win/lose outcome) entirely in the Stripe dashboard. Riservo does not build a dispute-evidence UI and does not gate booking access on dispute state. However, riservo DOES receive `charge.dispute.*` webhooks and surfaces each dispute to the business — see decision #35 for the exact surface (log + email admin + Pending Action deep-linking to Stripe). The booking's `payment_status` is informative, not load-bearing. A full in-dashboard dispute flow is deferred to v2. Locked.

### Existing schema and UI

26. **`payment_mode` enum values stay as-is**: `offline`, `online`, `customer_choice`. No enum change in this roadmap. Locked.
27. **Until this roadmap ships, the `online` and `customer_choice` options are hidden from Settings → Booking**. Session 1 ships the hide as its first checklist item. Session 5 lifts it. Between those two, the data column accepts all three values but the UI only exposes `offline`. Locked.
28. **`bookings.payment_status` column is already present** (cast to `App\Enums\PaymentStatus`). This roadmap extends the enum values to the final set: `not_applicable` (offline-from-the-start booking, no payment ever expected through riservo), `awaiting_payment` (pending Checkout outcome), `paid`, `unpaid` (customer attempted online in `customer_choice` mode but did not complete; payment due at appointment — distinct from `not_applicable` so the Business can see the audit trail and analytics can track conversion), `refunded`, `partially_refunded`, `refund_failed`. The pre-existing `pending` case is retired in Session 2a (no production data — riservo is pre-launch). A new column `bookings.payment_mode_at_creation` (string, default `'offline'` for new rows — NOT nullable; mirrors `Business.payment_mode` enum values at booking time) is also introduced in Session 2a to drive the failure-branching logic of decision #14 stably across subsequent Business setting changes. Every code path that creates a booking writes this column explicitly; decision #30's manual / google_calendar carve-out is the only place where the value deviates from the current `Business.payment_mode`. Locked.

### Post-review additions (status × payment × lifecycle edge cases)

29. **`confirmation_mode = manual` × online payment: charge upfront; webhook does NOT auto-confirm; admin approval promotes; admin rejection triggers automatic full refund — or, for the `pending + unpaid` variant, a refund-free cancel**. When a Business's `confirmation_mode = manual` AND `payment_mode_at_creation ∈ {online, customer_choice-with-online-pick}`, the `checkout.session.completed` webhook sets `payment_status = paid` but LEAVES `status = pending`. The booking is now "paid, awaiting admin confirmation". Admin "Confirm" promotes `status → confirmed` as today. Admin "Reject" (which until now has been a soft cancel) transitions `status → cancelled` AND dispatches `RefundService::refund($booking, null, 'business-rejected-pending')` for a full automatic refund. The public booking page and confirmation email make the contract explicit to the customer: "Your payment has been received. Your booking is pending the business's confirmation. If they cannot accept it, you will be refunded in full automatically." Rationale: (a) charging at booking aligns with decision #6 even under manual confirmation; (b) the customer's commitment is sealed before they leave the page; (c) the business is not forced to give up manual review; (d) rejection paths cannot strand customers' money. Gating online payment on `confirmation_mode = auto` was considered and rejected — too restrictive for legitimate hybrid operations.

    **`pending + unpaid` variant (customer_choice under manual confirmation, Checkout failed)**: a booking that lands at `pending + unpaid` — which arises only when Business = `customer_choice` AND `confirmation_mode = manual` AND the Checkout attempt failed / expired / was abandoned (see decision #14 for the branching) — takes a different rejection path than the `pending + paid` variant. An admin rejection cancels the booking WITHOUT dispatching `RefundService::refund` (there is nothing paid and thus nothing to refund). The existing customer-facing cancellation-notification template MUST branch on `payment_status`: for `payment_status = paid` (the happy `pending + paid` rejection path above) the notification includes "a full refund has been issued"; for `payment_status = unpaid` the notification omits the refund clause entirely — the copy reads as a plain cancellation. A template that unconditionally promises a refund would mislead the customer into expecting money back that was never charged. Session 2b's failure branching lands the booking at `pending + unpaid`; Session 3's rejection-path extension wires both templates and tests both branches. Locked.
30. **Manual bookings (`source = manual`) are always offline regardless of `Business.payment_mode`**. Staff-created bookings from the dashboard are created with `payment_mode_at_creation = 'offline'` and `payment_status = not_applicable`, and no Checkout session is created, irrespective of the Business's current `payment_mode`. Rationale: the customer is not in front of the staff member to authorise a charge; manual bookings exist specifically to capture phone / walk-in flows that bypass the public booking page. Charging the customer out-of-band is a v2 feature request (captured in BACKLOG as "Post-hoc online payment link for manual bookings" if it materialises). Google-calendar-sourced bookings (D-088) are also always `payment_mode_at_creation = 'offline'` / `payment_status = not_applicable` by the same logic — no payment ever expected. Locked.
31. **Reaper defense-in-depth against the webhook race**. The 2-minute reaper (decision #13) MUST NOT cancel a booking naively on `expires_at < now`. Three stacked mitigations:
    1. **Grace buffer**: reaper filters on `expires_at < now - 5 minutes` (not `< now`). Cheap hedge against webhook arrival delay within Stripe's normal retry envelope.
    2. **Pre-flight retrieve**: before cancelling, the reaper calls `stripe.checkout.sessions.retrieve($session_id, ['stripe_account' => $connectedAccountId])`. If Stripe reports `status = complete` OR `payment_status = paid`, the reaper treats the booking as paid-but-webhook-delayed, PROMOTES it inline (idempotent with the webhook path per decision #33), and skips the cancel. If Stripe reports `status = expired` or `status = open` AND `payment_status != paid`, the reaper proceeds with the cancel.
    3. **Late-webhook refund path**: if `checkout.session.completed` OR `payment_intent.succeeded` ever arrives for a booking whose `status = cancelled` AND whose `payment_status != paid`, the webhook handler dispatches an automatic full refund via `RefundService::refund($booking, null, 'cancelled-after-payment')`. The booking stays cancelled (slot may have been re-booked); the money goes back to the customer; a `payment.cancelled_after_payment` Pending Action is created AND an email is dispatched — both scoped to `business_members` with `role = admin` only (derived from decisions #19 / #35 — staff handle bookings, admins handle money). The email carries the customer's contact details, the dispatched refund ID, the original charge amount, and a deep-link to the Pending Action. Staff do not receive this notification.
    Together these three make it safe to run the reaper under any realistic webhook outage. Locked.
32. **Success page performs a synchronous Stripe retrieve and promotes inline**. The `success_url` controller does NOT rely solely on the webhook to have run. It calls `stripe.checkout.sessions.retrieve($session_id, ['stripe_account' => $connectedAccountId])` server-side, and if the session's `payment_status = paid` AND the local booking is still `awaiting_payment`, promotes the booking inline (idempotent with the webhook handler per decision #33). This eliminates the "success page shows `awaiting_payment`" UX race. The webhook remains the authoritative backstop (for customers who close the tab before the redirect and for server-to-server guarantees). Locked.
33. **Webhook and inline-promotion handlers must be idempotent at the outcome level, not only the event-ID level**. The D-092 event-ID dedup (inherited via decision #38's prefixed cache key) protects against Stripe replays, but every handler must additionally check DB state before acting — e.g., skip notification dispatch and skip DB writes if the booking is already `confirmed + paid`. This is a hard contract: a handler that writes to `paid_at` on every invocation will double-send notifications if the event cache is ever bypassed (maintenance replay, cache flush, local dev). The shape is: `if ($booking->payment_status === PaymentStatus::Paid) { return; }` guards at the top of every promotion path, and analogous guards for refund / cancellation handlers. Locked.
34. **`account.updated` and `account.application.deauthorized` handlers re-fetch account state via the Stripe API, not the event payload**. Stripe does not guarantee event ordering; two `account.updated` events arriving out of order would otherwise leave the row in the stale payload's state. Treat every `account.*` webhook as a "something changed" nudge; on receipt, call `stripe.accounts.retrieve($accountId)` and persist whatever Stripe authoritatively reports now. The same pattern applies to `capability.updated` if we ever subscribe to it. Locked.
35. **Dispute webhooks surface a Pending Action (+ email) to admins only**. Events `charge.dispute.created`, `charge.dispute.updated`, and `charge.dispute.closed` are subscribed on the Connect webhook endpoint. On `charge.dispute.created`: (a) log the event; (b) email the Business's admins — scoped to `business_members` with `role = admin` only, matching decision #19's refund-initiator rationale (disputes are commercial-decision surfaces; staff handle day-to-day bookings, admins handle money) — with the dispute reason and a deep-link to the dispute evidence UI in the Stripe Express dashboard; (c) create a Pending Action row (on the generic `pending_actions` table per decision #44) visible to admins only in the dashboard until the dispute is closed. Staff (with or without a linked Provider row) do not receive dispute emails and do not see dispute Pending Actions in their dashboard view. On `charge.dispute.closed`: resolve the Pending Action with a "Dispute won" / "Dispute lost — funds returned to customer" summary and email admins with the outcome. No evidence-upload UI in riservo — that stays in Stripe. The booking's existing `status` and `payment_status` do not change on dispute events; `payment_status` remains `paid` throughout (payment_status is informative, per decision #25). Locked.
36. **Refunds attempt against the cached `stripe_account_id` even after disconnect; failure surfaces a Pending Action (admin-only) with email dispatch; refund idempotency keys are deterministic**. When a connected account row is soft-deleted (disconnect, Session 1), its `stripe_account_id` is retained on the row for audit. Automatic refund attempts (customer in-window cancel, business cancel, admin rejection of a paid pending) still call `stripe.refunds.create([...], ['stripe_account' => $cachedAccountId])`. If Stripe returns a permission error (application deauthorized, account fully suspended), the refund is marked `refund_failed` with the verbatim Stripe error message, the customer-facing UI reads "Refund not automatic — the business will contact you shortly", and a `payment.refund_failed` Pending Action is created AND an email dispatched — both scoped to `business_members` with `role = admin` only (derived from decision #19 — money is an admin-only commercial surface; staff do not receive refund-failure notifications and do not see refund-failed Pending Actions in their dashboard view). The email carries the customer's contact details, the outstanding amount, the verbatim Stripe error message, and a deep-link to the Pending Action. The admin can resolve it by re-connecting Stripe (which retries the refund) or by refunding offline and clicking "Mark as refunded offline" on the Pending Action (which closes it without a Stripe write). **Idempotency-key formula — one `booking_refunds` row per refund ATTEMPT, row UUID is the key**: `RefundService::refund` creates a `booking_refunds` row with `status = pending` BEFORE calling Stripe. The row carries a dedicated UUID column (populated at insert time); the Stripe call issues `stripe.refunds.create([...], ['idempotency_key' => 'riservo_refund_'.$bookingRefund->uuid])`. Retries of the SAME attempt (scheduler re-run mid-response, admin double-click on the same submission, webhook re-delivery during the Stripe call) reuse the row and therefore the UUID — Stripe collapses them. Two LEGITIMATELY-DISTINCT refund intents on the same booking (admin issues a $50 partial, admin later issues another $50 partial) get two rows and two UUIDs, which is correct — the partial-refund clamp in decision #37 enforces that the second $50 is allowed because $50 of the original $100 remains refundable. Synthetic hashes of `(booking_id, amount_cents, initiator)` would collapse the second attempt into the first incorrectly; the row-UUID approach avoids that foot-gun by construction. The `booking_refunds.uuid` column is introduced in Session 2b alongside the table itself. Locked.
37. **Refund amounts are always computed from `bookings.paid_amount_cents`, never from `Service.price`**. The booking row's `paid_amount_cents` is the authoritative refundable amount. `Service.price` may have changed between booking creation and cancellation; using the current price would either over- or under-refund. Partial refunds are bounded by `paid_amount_cents - SUM(booking_refunds.amount_cents WHERE status IN (pending, succeeded))`. The partial-refund UI dialog enforces this client-side; the `RefundService` enforces it server-side as a second check. Locked.
38. **Connect webhook cache-key prefix is distinct from the subscription webhook**. MVPC-3's subscription webhook uses cache key `stripe:event:{id}` (D-092). The Connect webhook uses `stripe:connect:event:{id}`. The two namespaces cannot collide even if Stripe ever emits the same event ID across the platform and a connected account (currently impossible, but the extra safety is free). The D-092 shared helper takes a prefix parameter; both callers pass their own prefix. Locked.
39. **Checkout sessions pass the customer's current app locale**. The public booking page renders in IT / DE / FR / EN per D-008. When creating a Checkout session, the controller passes `locale` derived from `app()->getLocale()` so the hosted Checkout page matches the customer's booking-flow language. Stripe's supported locales are checked at plan time; unsupported locales fall back to `auto` (Stripe's browser-based detection). Locked.
40. **A paid booking for a service that is subsequently deactivated remains fully valid**. Deactivating a service (`is_active = false`) does not affect existing bookings of that service — the booking row still resolves `service_id`, and the refund path uses `paid_amount_cents` (decision #37), not the service record. The customer's appointment proceeds; customer-side cancellation inside the window still triggers an automatic refund; the admin can still refund manually. No special handling in the service-deactivation flow is needed. This is called out explicitly so no future refactor introduces a default scope hiding inactive services from the booking's relation. Locked.
41. **Async-payment-method semantics confirmed at Session 2a plan time**. Stripe Checkout emits `checkout.session.async_payment_succeeded` / `checkout.session.async_payment_failed` for payment methods that settle asynchronously (delayed bank debits, some SEPA flows). TWINT in our configuration is nominally synchronous — `checkout.session.completed` arrives with `payment_status = paid`. The Session 2a implementing agent MUST confirm this against current Stripe behaviour before writing the happy-path handler. If any configured method is actually async, the booking stays in `awaiting_payment` after `checkout.session.completed` and is promoted only on `payment_intent.succeeded` / `checkout.session.async_payment_succeeded`. The decision lock is: **the handler branches on `payment_status` of the retrieved session, not on the event name alone**. Locked.
42. **Refunds preserve the booking row's captured `currency`, not the connected account's current default**. A connected account may change its default currency (rare, but possible via Stripe support). The refund API takes the charge ID and the amount in the original charge's currency; our refund service reads `bookings.currency` (captured at booking creation per decision #28's extended columns) and passes it through. This also means partial-refund UI shows amounts in the booking's captured currency, not whatever the account currently reports. Locked.
43. **Non-CH connected accounts may not enable `online` or `customer_choice` in MVP; the gate is config-driven and the seams are coded open for a fast-follow extension**. TWINT is CH-only; this roadmap's copy, UX, and tax assumptions (decision #11) are all CH-centric. The country gate is expressed through `config('payments.supported_countries')` — a new config entry whose value in MVP is `['CH']`. No hardcoded `'CH'` literal appears anywhere in application code, tests, or Inertia props; every call site reads the config. Session 5's Settings → Booking gate uses `in_array($connectedAccount->country, config('payments.supported_countries'))`; Session 4's payout page uses the same helper for its non-CH banner; the Session 2a public-booking `PublicBookingController::store` path and any future retry / admin-triggered Checkout path **also** asserts the country is in the supported set server-side (defense-in-depth against DB tampering, migration bugs, and a `payment_mode_at_creation` drift class of bug). A failing assertion returns a 422 and emits an admin-visible log line naming the offending connected-account ID and country; the Checkout session is never created. Session 1's onboarding does NOT block non-CH countries from connecting an account (they may still want to track verification state); the config-driven gate is the single functional enforcement point across Sessions 2a → 5. Extending to IT / DE / FR / AT / LI in a fast-follow roadmap becomes a config change + TWINT-to-card fallback verification + locale-list audit (decision #39), not a refactor. The BACKLOG entry "Online payments for non-CH connected accounts" names the seams-open baseline so the future session knows what is already prepared. Locked.
44. **Pending Actions table + model are generalised in Session 1; dashboard surfaces remain per-session, no unified list**. The existing `calendar_pending_actions` table is renamed to `pending_actions` in Session 1's data-layer step; `integration_id` becomes nullable so payment Pending Actions (which have no calendar integration) can coexist with calendar ones on the same table. Type-specific fields ride in the existing `payload` JSON column. Existing `PendingActionType` enum values (`riservo_event_deleted_in_google`, `external_booking_conflict`) are NOT renamed in this roadmap — naming symmetry with the new `payment.*` cases (`payment.dispute_opened`, `payment.refund_failed`, `payment.cancelled_after_payment`) is a post-MVP polish, not a PAYMENTS task. Session 1 audits every dereference of `integration_id` in `CalendarPendingActionController`, `PullCalendarEventsJob`, the Inertia props on the calendar pending-actions list, and any shared model query; wherever calendar-specific behaviour is implied, an explicit `whereNotNull('integration_id')` (or `->where('type', PendingActionType::ExternalBookingConflict)` style filter) is added so nullable rows do not leak into calendar-only surfaces. **Dashboard surfaces for payment Pending Actions are specified per-session**: Session 2b renders `payment.cancelled_after_payment` as a booking-detail banner (admin-only); Session 3 surfaces `payment.dispute_opened` via a dispute panel on booking detail + deep-link to Stripe (admin-only) and `payment.refund_failed` via a booking-detail banner (admin-only). A unified cross-family `PendingActionController` + list UI is a post-MVP polish, not a PAYMENTS task; the existing `CalendarPendingActionController` keeps operating against `pending_actions` with the type-scoped filter. Payment PAs are visible only to admins per decisions #31 / #35 / #36; calendar PAs keep their existing visibility (calendar-integration-owner + admin per D-088). Locked.
45. **Every authenticated dashboard controller, query, and job in this roadmap scopes via `App\Support\TenantContext` (D-063)**. The new `Dashboard\Settings\ConnectedAccountController`, `Dashboard\BookingRefundController`, and `Dashboard\PayoutsController` read the active business via `tenant()->business()` and the active role via `tenant()->role()`; every Eloquent query on business-owned data is scoped through a tenant-aware relation (`$business->bookings()`, `$business->stripeConnectedAccount`) or an explicit `->where('business_id', tenant()->businessId())`. Pending Action resolution handlers scope via `tenant()->businessId()` (they already do for calendar PAs per D-088's scoping clause — payment PAs inherit the same pattern). Any queued work dispatched in this roadmap reads the target business via the booking's FK, not from the dispatcher's session context. **Only two paths operate outside tenant context**: (a) the public booking controller, which resolves the business from the URL slug and writes all booking columns with the resolved `business_id`; (b) the Stripe Connect webhook controller, which resolves the target booking / business from `client_reference_id` or `metadata.riservo_business_id` and scopes all DB writes by those authoritative FKs. Cross-tenant access is a 403 (authz) or 404 (tenant-scoped `findOrFail`) at the boundary — never a silent read, never a silent write. Implementing agents for Sessions 1, 3, and 4 assert this on every new controller they introduce; feature tests include at minimum one cross-tenant denial test per new controller. Locked.

---

## Session 1 — Stripe Connect Express Onboarding

**Owner**: single agent. **Prerequisites**: MVPC-3 closed (Cashier is installed, so the Stripe SDK, webhook plumbing pattern, and env conventions exist). **Unblocks**: Session 2.

Stand up the connected-account onboarding flow in isolation. Deliverable is a verified Stripe Express round-trip: an admin clicks "Enable online payments", lands on Stripe-hosted KYC, completes it, and returns to riservo with a connected account persisted and a verification state reflected in the dashboard. No charging, no refunds, no payouts — this session is pure onboarding + webhook reception.

### Hide `online` / `customer_choice` from Booking Settings (do first)
- [ ] Settings → Booking: filter the `paymentModeItems` array in `resources/js/pages/dashboard/settings/booking.tsx` (today at lines 51–55) to the `offline` option only. `online` and `customer_choice` are absent from the select. Remove the "coming soon" hint copy below the select — no date-promising UI in the interim. Any existing `online` / `customer_choice` value persisted on a Business row (seeded or set manually in the DB for dogfooding) must still read back without error: the select silently falls back to the persisted value on load even though the UI cannot change it to one of the hidden options.
- [ ] Session 5 lifts this restriction; every session up to and including Session 4 keeps the hide in place.

### Data layer
- [ ] New table `stripe_connected_accounts` keyed by `business_id` (unique, per locked decision #20). Columns: `id`, `business_id`, `stripe_account_id`, `country` (ISO-3166-1 alpha-2), `charges_enabled`, `payouts_enabled`, `details_submitted`, `requirements_currently_due` (JSON), `requirements_disabled_reason` (nullable string), `default_currency`, `created_at`, `updated_at`.
- [ ] `Business` model gains `hasOne(StripeConnectedAccount::class)` and a `canAcceptOnlinePayments(): bool` helper that returns `charges_enabled && payouts_enabled && details_submitted`.
- [ ] `StripeConnectedAccount` model with casts and a `verificationStatus(): string` returning one of `pending`, `incomplete`, `active`, `disabled` (plain strings, not an enum — Stripe's own verification states are the source of truth and are richer than a local enum can meaningfully carry).
- [ ] **Pending Actions generic rename (locked decision #44)**: migration renames table `calendar_pending_actions` → `pending_actions`; alters `integration_id` to `nullable`; preserves all existing rows and their types verbatim (no enum-value rename in this roadmap). Update `App\Models\PendingAction::$table` accordingly. Audit every reader — `CalendarPendingActionController`, `PullCalendarEventsJob`, the Inertia pending-actions list props, `HandleInertiaRequests` if it surfaces PAs, and any shared query scope — and add `whereNotNull('integration_id')` (or a type-based filter) wherever the code path assumes a calendar PA shape. The dashboard route / URL / controller rename to a generic `PendingActionController` is out-of-scope for this roadmap (tracked as a post-MVP polish); the existing `CalendarPendingActionController` keeps operating against `pending_actions` with the type-scoped filter.
- [ ] **New config file `config/payments.php`** (locked decision #43): three keys — (a) `supported_countries` (MVP value `['CH']`, env `PAYMENTS_SUPPORTED_COUNTRIES` comma-separated), used by the Session 2a country assertion, Session 4's payout-page non-CH banner, and Session 5's Settings gate; (b) `default_onboarding_country` (MVP value `'CH'`, env `PAYMENTS_DEFAULT_ONBOARDING_COUNTRY`), used by `ConnectedAccountController::create` as the fallback when `Business.address` does not resolve to a country; (c) `twint_countries` (MVP value `['CH']`, env `PAYMENTS_TWINT_COUNTRIES` comma-separated), used by Session 2a's Checkout `payment_method_types` branching — TWINT is CH-only today but this is the seam for Stripe adding new TWINT markets or for non-CH countries in `supported_countries` that fall back to card-only. No hardcoded `'CH'` literal may appear in any application code, test, Inertia prop, or Tailwind class check — every site reads the config.

### Controllers and routes
- [ ] `Dashboard\Settings\ConnectedAccountController` with actions `show`, `create`, `refresh`, `disconnect`. Scoped to admins only (not staff) — this is a commercial decision.
- [ ] `create()` calls the Stripe API to create an Express account in the Business's country (derived from `Business.address`, falling back to `config('payments.default_onboarding_country')` — MVP value `'CH'`; no hardcoded literal per locked decision #43), persists the `stripe_connected_accounts` row, and returns an Inertia redirect to the Stripe-generated Account Link onboarding URL. The Account Link is created with BOTH `refresh_url` and `return_url` pointing at the `refresh()` action — Stripe triggers `refresh_url` when the onboarding link expires mid-flow (the user re-lands, we re-generate a fresh Account Link) and `return_url` when the user completes or abandons (we pull the latest state and surface the appropriate banner). One controller action, two URL slots, same handler.
- [ ] `refresh()` is the return URL (and refresh URL) after onboarding — pulls the latest account state via Stripe API and persists it; if Stripe reports the Account Link expired (user took > a few minutes in KYC), transparently generates a fresh Account Link and redirects back into Stripe-hosted onboarding; otherwise redirects to the Settings page with a success or "continue onboarding" banner.
- [ ] `disconnect()` follows locked decision #19: deletes the `stripe_connected_accounts` row (or soft-deletes it — implementation agent decides; the user-visible effect is the same), forces `payment_mode = offline` on the Business, writes an audit row if audit logging is in place.

### Webhook reception
- [ ] `POST /webhooks/stripe-connect` endpoint, CSRF-excluded, signature-verified against a dedicated Connect webhook secret (distinct from the MVPC-3 subscription webhook endpoint). The secret lives at `config('services.stripe.connect_webhook_secret')`, reading env var `STRIPE_CONNECT_WEBHOOK_SECRET`. Both the env var and the config key are added to `docs/DEPLOYMENT.md` in this session's ops checklist.
- [ ] Event `account.updated`: per locked decision #34, the handler treats the event as a nudge only and calls `stripe.accounts.retrieve($accountId)` to pull fresh state. It then persists `charges_enabled`, `payouts_enabled`, `details_submitted`, `requirements`, `requirements.disabled_reason`, `default_currency`. If `charges_enabled` transitions from true to false (or the account row is newly insufficient per `canAcceptOnlinePayments()`), force `payment_mode` back to `offline` (locked decision #20).
- [ ] Event `account.application.deauthorized`: also re-fetches via API (if permissions still allow — otherwise marks the row as deauthorized) and treats identically to `disconnect()` while retaining `stripe_account_id` on the soft-deleted row per locked decision #36.
- [ ] **Dispute webhook subscriptions (reserved for Session 3)**: `charge.dispute.created`, `charge.dispute.updated`, `charge.dispute.closed` are added to the subscribed event list in the Stripe dashboard during Session 1's infrastructure step, so operators configure them once; Session 3 writes the actual handlers per locked decision #35. Session 1 routes any premature dispute event to a log-and-200 no-op handler to avoid Stripe retry storms during the window between roadmap sessions.
- [ ] Idempotency: process each event at-most-once using the D-092 cache-dedup pattern with prefix `stripe:connect:event:` (locked decision #38). **The current codebase inlines the dedup in `StripeWebhookController::handleWebhook` with a private constant `DEDUP_PREFIX = 'stripe:event:'`; there is no extractable helper today.** Session 1 extracts the dedup into a small shared helper (trait `App\Support\Billing\DedupesStripeWebhookEvents` or a helper class method — implementing agent decides) taking a cache-prefix parameter, and refactors the MVPC-3 `StripeWebhookController` to pass `stripe:subscription:event:` explicitly. The new Connect webhook controller (see Gap-3 clarification below) is a **fresh** controller — it is NOT a subclass of Cashier's `WebhookController`, because Cashier's base routes platform-level subscription events (`customer.subscription.updated`, `invoice.payment_succeeded`, …) which do not match the Connect event set (`checkout.session.*`, `account.*`, `charge.*`). Both controllers use the same trait / helper with their own prefix.
- [ ] Per locked decision #33, every handler is outcome-level idempotent. For payload-driven events (e.g., `checkout.session.completed`), the DB-state guard at the top reads `if ($booking->payment_status === PaymentStatus::Paid) { return 200; }`. For events that are treated as nudges per locked decision #34 (`account.updated`, `account.application.deauthorized`), the handler re-fetches authoritative state via `stripe.accounts.retrieve($accountId)` FIRST, then guards `if ($row->matches($fetched)) { return 200; }` so identical-state re-deliveries skip writes without relying on the webhook payload. The payload is not trusted as the source of truth for account state.

### Dashboard UI
- [ ] `Dashboard/settings/connected-account.tsx` (new page under Settings, admin-only nav item, shown always once this session lands).
- [ ] Not-connected state: "Enable online payments" CTA; short explainer that onboarding is Stripe-hosted; required-info preview. Copy calls out explicitly that a user admining multiple businesses must onboard each Business independently — one connected account per Business per locked decision #22 — and that each Business gets its own Stripe Express dashboard login.
- [ ] Onboarding-incomplete state: "Continue Stripe onboarding" CTA back to a fresh Account Link; list of `requirements_currently_due` if Stripe exposes them.
- [ ] Active state: read-only summary (country, default currency, charges enabled, payouts enabled, Stripe account ID last 4 for support); "Disconnect" button with confirmation dialog.
- [ ] Disabled / disabled-by-Stripe state: error banner with `requirements_disabled_reason` verbatim from Stripe; "Contact support" CTA.
- [ ] Dashboard-wide banner on every page when `canAcceptOnlinePayments()` is false AND `payment_mode !== 'offline'` (can only happen transiently between a webhook demotion and a UI refresh; still worth surfacing).
- [ ] Accessibility pass on all Session 1 pages: all interactive controls keyboard-reachable; CTAs have descriptive aria-labels; status banners use `role="alert"` / `role="status"` appropriate to severity; colour is never the sole carrier of status (verified / incomplete / disabled chips also carry an icon or text).

### Tests + ops
- [ ] Feature tests: admin can create connected account (Stripe SDK faked); staff cannot reach the page (403); return-URL refresh persists state; `account.updated` webhook re-fetches via API and updates the row; `account.updated` with stale payload (older `updated` event arriving after a newer one) converges to Stripe-authoritative state (no stale overwrite) per decision #34; signature validation rejects unsigned requests; disconnect forces `payment_mode = offline`; disconnected row retains `stripe_account_id` per decision #36; multi-business admin creates one account per business; cache-key prefix isolation between `stripe:connect:event:` and `stripe:subscription:event:` (same event ID on both endpoints dedupes independently); **cross-tenant denial (locked decision #45)** — admin of Business A cannot view / refresh / disconnect Business B's connected account (404 via tenant-scoped lookup).
- [ ] **FakeStripeClient contract** extended explicitly — the `tests/Support/Billing/FakeStripeClient` established in MVPC-3 grows two distinct mock categories, because the Stripe API splits cleanly between platform-level calls (header absent) and connected-account-level calls (header required):
  - **Platform-level** (MUST assert `stripe_account` header is ABSENT from per-request options): `mockAccountCreate($params, $response)`, `mockAccountRetrieve($accountId, $response)`, `mockAccountLinkCreate($params, $response)`, `mockLoginLinkCreate($accountId, $response)`. These act on platform-owned resources; the account ID is in the URL path, not the `Stripe-Account` header.
  - **Connected-account-level** (MUST assert `stripe_account => $accountId` is PRESENT in per-request options): `mockCheckoutSessionCreateOnAccount`, `mockCheckoutSessionRetrieveOnAccount` (Session 2a), `mockRefundCreate` (Session 2b — the late-webhook refund moved here from Session 3), `mockTaxSettingsRetrieve` (Session 4), `mockBalanceRetrieve`, `mockPayoutsList` (Session 4). `mockRefundCreate` additionally asserts the `idempotency_key` per-request option matches `'riservo_refund_'.{row_id}` per decision #36.
  - A call that crosses categories (e.g., Checkout session create with no header, or `accounts.create` with a header) is a test failure. The FakeStripeClient helper encodes this split and the Session 1 implementing agent wires it in. Subsequent sessions extend only their own method list — the platform-vs-account distinction is fixed here.
- [ ] **Pending Action infrastructure**: reuse MVPC-2's D-088 pattern. If a generic `pending_actions` table already exists, extend its `type` enum with values consumed by Session 3 (`payment.dispute_opened`, `payment.refund_failed`, `payment.cancelled_after_payment`); if not, Session 1 stands up the minimum shape now so Session 3 can write rows without a schema session. The session's plan decides the exact shape at plan time.
- [ ] `php artisan wayfinder:generate` after route additions.
- [ ] Update `docs/DEPLOYMENT.md` with: Connect webhook endpoint URL and secret, the full subscribed-event list (account.updated, account.application.deauthorized, charge.dispute.created, charge.dispute.updated, charge.dispute.closed, plus Session 2/3 events), test-vs-live Connect keys, env var inventory.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.

**Out of scope**: taking actual payments, refunds, payout surfacing, lifting the Settings → Booking `payment_mode` hide, embedded onboarding, the dispute handler body (subscription only — handler lives in Session 3). All of that lives in later sessions.

---

## Session 2a — Payment at Booking (Happy Path)

**Owner**: single agent. **Prerequisites**: Session 1. **Unblocks**: Session 2b.

Wire the happy-path customer charge: on the public booking flow, when the Business has `payment_mode = online` (or `customer_choice` with customer having picked online), the "Confirm booking" CTA creates a `pending + awaiting_payment` booking + a Stripe Checkout session, redirects the customer to the hosted page, and BOTH the `checkout.session.completed` webhook AND a synchronous retrieve on the success page promote the booking to `confirmed + paid`. This session is deliberately scoped to the happy path; failure branching, reaper, and admin surface are Session 2b.

**Note**: `payment_mode = online` / `customer_choice` is still hidden from Settings; for testing and dogfooding this session, the Business's `payment_mode` is seeded directly in the DB.

### Data layer
- [ ] **No production data migration concerns** — riservo is pre-launch. Dev-database seed rows (as of 2026-04-19: 112 `pending` + 2 `paid` payment_status rows) are fully resettable via `php artisan migrate:fresh --seed`. The Session 2a migration may assume fresh state; no defensive backfill logic is required for existing `payment_status` values. The old `PaymentStatus::Pending` case is retired without a rename / redirect.
- [ ] Extend `App\Enums\PaymentStatus` to the final set `not_applicable`, `awaiting_payment`, `paid`, `unpaid`, `refunded`, `partially_refunded`, `refund_failed` (per locked decision #28). The existing `Pending` case is removed. The implementing agent picks the case-declaration order that serialises cleanly for the Inertia payload and the admin filter chips.
- [ ] Migration: `bookings` gains `stripe_checkout_session_id` (nullable unique string), `stripe_payment_intent_id` (nullable unique string), `stripe_charge_id` (nullable unique string), `paid_amount_cents` (nullable integer), `currency` (nullable ISO-4217 3-char, captured at booking creation from `stripe_connected_accounts.default_currency` — NOT from the Service or the Business, since the booking's currency is whatever Stripe will settle in; this is the value decision #42's refund path reads), `paid_at` (nullable timestamp), `payment_mode_at_creation` (string, default `'offline'` for every new row; mirrors `Business.payment_mode` enum values, captured at booking time per locked decision #14; not nullable on new rows — every code path that creates a booking must write this column), `expires_at` (nullable timestamp; populated only when `payment_mode_at_creation = online`, per locked decision #13).
- [ ] `Booking` model casts and fillable updated; a `isOnlinePayment()` helper, a `paidAmount(): ?Money` (or plain decimal) accessor, a `wasCustomerChoice(): bool` helper reading `payment_mode_at_creation === 'customer_choice'`, and a `remainingRefundableCents(): int` helper that enforces the decision #37 invariant (clamps to `paid_amount_cents - sum(non-failed booking_refunds.amount_cents)`). The helper returns `paid_amount_cents` in 2a and is expanded in 2b once `booking_refunds` lands (Session 2b's Data layer creates the table alongside the minimal `RefundService`).

### Public booking flow
- [ ] Public booking page: when the Business's `payment_mode` is `online`, the "Confirm booking" CTA text becomes "Continue to payment"; when `customer_choice`, the final step exposes an inline "Pay now / Pay on site" choice (UI pattern = agent's call).
- [ ] `PublicBookingController::store` (existing) is extended: when the resolved booking would use online payment, the booking is created with `status = pending`, `payment_status = awaiting_payment`, `payment_mode_at_creation` set to the Business's `payment_mode` at this exact moment (snapshot — never re-read), and `expires_at` set to `now + 90 minutes` ONLY IF `payment_mode_at_creation = online` (left null for `customer_choice`, per locked decision #13). The booking row is committed in a DB transaction (so the GIST invariant per D-066 fires before any Stripe call); on commit, a Stripe Checkout session is created on the connected account using the `Stripe-Account` header and the `stripe_checkout_session_id` is persisted via a follow-up UPDATE. If Stripe session creation fails after the booking is committed, the booking is marked cancelled immediately so the slot releases — this failure path is tested.
- [ ] **Server-side country assertion (locked decision #43 defense-in-depth)**: before creating a Checkout session, the controller asserts `in_array($business->stripeConnectedAccount->country, config('payments.supported_countries'))`. A failing assertion fails closed: booking row is marked cancelled (slot freed), the customer sees a 422 "Online payments aren't available for this business right now — please contact them directly", and an admin-visible warning is logged naming the connected-account ID, the offending country, and the configured supported set. This defends against DB tampering, migration bugs, and a `payment_mode_at_creation` drift class of bug independent of Session 5's UI gate. Same assertion pattern is applied to any future Checkout-creation path (retry endpoint, admin-triggered Checkout).
- [ ] Checkout session configuration: `payment_method_types = [card, twint]` when the connected account country is in `config('payments.twint_countries')` (MVP value `['CH']`, Session 1 owns the config); `card` only when the country is in `config('payments.supported_countries')` but NOT in `config('payments.twint_countries')` — in MVP today the two config sets are identical so this branch is unreachable, but it is wired for the non-CH fast-follow per locked decision #43. No hardcoded `'CH'` literal in this branching logic. `mode = payment`; line item derived from `Service.name` + `paid_amount_cents`; `success_url` and `cancel_url` route back to riservo; `client_reference_id = booking.id`; `metadata.riservo_booking_id` + `metadata.riservo_business_id` + `metadata.riservo_payment_mode_at_creation` (so webhook handlers can branch even before a DB lookup); `locale` derived from `app()->getLocale()` with fallback to `auto` for unsupported locales, per locked decision #39; `expires_at = now + 90min` (mirrors the booking row); direct charges via the `Stripe-Account` header per locked decision #5; `payment_intent_data.transfer_data` is NOT used; no `application_fee_amount`.
- [ ] A service with `Service.price === null` or `0` forces the offline path regardless of Business `payment_mode` (per locked decision #8): no Checkout session is created and `payment_status = not_applicable`. However `payment_mode_at_creation` still MIRRORS the Business's current `payment_mode` per the snapshot invariant in decision #14 (NOT forced to `'offline'` — the booking correctly reflects that the Business offers online payment even though this specific service wasn't chargeable online). A downstream reader distinguishes "no online payment attempted" via `payment_status = not_applicable` AND the absence of `stripe_checkout_session_id`, not via the snapshot column. Decision #30 (manual / google_calendar always `'offline'`) remains the single carve-out to the mirror rule.
- [ ] Manual bookings (`source = manual`) and Google-calendar bookings (`source = google_calendar`) are always created with `payment_mode_at_creation = 'offline'` and `payment_status = not_applicable`, irrespective of `Business.payment_mode` (locked decision #30). The controller paths that create these bookings write the columns explicitly at insertion time.

### Webhook handling (Connect endpoint extended from Session 1)
- [ ] `checkout.session.completed` (happy path only — failure events belong to Session 2b): look up booking by `client_reference_id`; verify the session's `payment_status = paid` before acting (per locked decision #41 the handler branches on the session's payment_status, not the event name alone); transition to `status = confirmed` + `payment_status = paid`; persist `stripe_payment_intent_id`, `stripe_charge_id`, `paid_amount_cents`, `currency`, `paid_at`; clear `expires_at`; dispatch the existing booking-confirmed notifications (suppressed for `source = google_calendar` per D-088, not relevant here but noted).
- [ ] **`confirmation_mode = manual` branch (locked decision #29)**: when the owning Business's `confirmation_mode = manual`, the handler sets `payment_status = paid` but LEAVES `status = pending`. Notifications fired are the "payment received, awaiting business confirmation" variant (new template — reuses `BookingReceivedNotification`'s shell with a dedicated context value per D-057). The booking stays in the admin's pending queue for approval; the admin-approval → confirmed transition is an existing dashboard path.
- [ ] Outcome-level idempotency (locked decision #33): every handler begins with a DB-state guard — e.g., `if ($booking->payment_status === PaymentStatus::Paid) { return 200; }` — so replays or inline-promotion races never double-write and never re-dispatch notifications.
- [ ] The handler also handles the `checkout.session.async_payment_succeeded` event symmetrically for any async payment method that lands in scope (per locked decision #41, TWINT expected synchronous but verify at plan time; if async, the booking stays `awaiting_payment` until this event).

### Success-page synchronous retrieve (locked decision #32)
- [ ] The `success_url` controller calls `stripe.checkout.sessions.retrieve($session_id, ['stripe_account' => $connectedAccountId])` server-side. If the returned session has `payment_status = paid` AND the local booking is still `awaiting_payment`, the controller promotes the booking inline using the same code path as the webhook handler (guarded by the decision #33 DB-state check so double-promotion is a no-op). The page then renders the confirmed state synchronously — no polling, no "payment pending" flicker.
- [ ] If the retrieve fails (Stripe timeout, network error) the controller falls back to rendering the current booking state and surfacing a "Still processing — check back in a moment" banner; the webhook remains the authoritative backstop.

### Tests + ops
- [ ] Feature tests: happy path for `payment_mode_at_creation = online` — booking created `pending + awaiting_payment`, Checkout session created with the Stripe-Account header asserted, webhook promotes to `confirmed + paid`, booking-received + booking-confirmed notifications fire exactly once; happy path for `customer_choice` with customer picking online — same promotion; happy path for `customer_choice` with customer picking offline — no Checkout session, booking is `confirmed + not_applicable` as today; `Service.price = null` forces offline path; manual booking creates `offline + not_applicable` regardless of Business.payment_mode (locked decision #30); Google-calendar booking creates `offline + not_applicable`.
- [ ] Success-page retrieve tests: return controller promotes the booking inline when the webhook has not yet run; return controller is a no-op when the webhook has already run (idempotent); return controller renders a "processing" state when Stripe retrieve fails; return controller ignores the booking if the local row's `payment_status` is already terminal.
- [ ] `confirmation_mode = manual` test: on `checkout.session.completed`, booking is `pending + paid` (status stays pending); admin "Confirm" promotes to `confirmed`; the Session 3 rejection-refund path is stubbed here and tested in Session 3.
- [ ] Slot lock during Checkout: GIST constraint rejects an overlapping booking attempt with the expected 409 while the first booking is `pending + awaiting_payment`.
- [ ] Idempotency test: replaying a `checkout.session.completed` event with the same Stripe event ID hits the `stripe:connect:event:` cache; replaying a promoted booking's event with a FRESH (unseen) event ID also does not double-promote because of the decision #33 DB-state guard.
- [ ] Snapshot test: the `payment_mode_at_creation` column on a booking row does not change when the Business's `payment_mode` changes after booking creation.
- [ ] Snapshot-mirror test (locked decision #14 three-outcome invariant): for Business = `customer_choice`, all three customer outcomes write `payment_mode_at_creation = 'customer_choice'` — (a) picks "pay now" + Checkout succeeds → `'customer_choice'`; (b) picks "pay now" + Checkout fails → `'customer_choice'`; (c) picks "pay on site" upfront (no Checkout session created) → `'customer_choice'` (NOT `'offline'`). A regression here would make customer_choice-offline-pick bookings indistinguishable from pure-offline bookings in the Session 2b dashboard filter.
- [ ] Locale test: Checkout session creation passes the app's current locale; unsupported locale falls back to `auto`.
- [ ] **Locale invariant test** (early-warning against Stripe silently dropping a D-008 locale): for each locale in `['it', 'de', 'fr', 'en']`, the resolver used by the Checkout controller returns that locale unchanged — never falls through to `'auto'`. If Stripe ever retires a supported locale, this test fires with a clear message pointing at locked decision #39 so the drift is surfaced before a customer sees a mismatched-language handoff.
- [ ] **Async-payment handler test** (locked decision #41 hedge): whether TWINT is synchronous or async at implementation time, the handler branches on `$session->payment_status === 'paid'` — NOT on the event name alone. Test covers both `checkout.session.completed` with `payment_status = paid` (synchronous promote) and `checkout.session.completed` with `payment_status = unpaid` followed by `checkout.session.async_payment_succeeded` (async promote). A future Stripe change that flips TWINT to async cannot silently regress the promotion path.
- [ ] FakeStripeClient extended with `mockCheckoutSessionCreateOnAccount` and `mockCheckoutSessionRetrieveOnAccount`; both assert presence of the `stripe_account` header.
- [ ] Accessibility pass on the updated public booking flow and the payment-success / processing states.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.
- [ ] Update `docs/DEPLOYMENT.md` with: Checkout session env, webhook events to subscribe for Session 2a scope, TWINT test flow notes.

**Out of scope**: failure webhooks (`checkout.session.expired`, `checkout.session.async_payment_failed`, `payment_intent.payment_failed`), reaper job, cancel_url UX, admin dashboard touch-ups — all Session 2b. Refunds (Session 3), payouts (Session 4), Settings toggle lift (Session 5).

---

## Session 2b — Payment at Booking (Failure Branching + Admin Surface)

**Owner**: single agent. **Prerequisites**: Session 2a. **Unblocks**: Session 3.

Wire the Checkout failure paths, the reaper with defense-in-depth, and the admin-facing payment surface. Builds on Session 2a's data layer and happy-path handler. Also introduces the minimal `booking_refunds` table and `RefundService` skeleton needed to deliver decision #31's late-webhook refund end-to-end (not stubbed); Session 3 expands the service.

### Data layer

- [ ] New table `booking_refunds`: `id`, `uuid` (unique, populated at insert time — the source of the Stripe idempotency key per locked decision #36), `booking_id` (FK, indexed), `stripe_refund_id` (nullable unique — null until Stripe accepts the create), `amount_cents`, `currency`, `status` (`pending`, `succeeded`, `failed`), `reason` (`cancelled-after-payment` is the only reason Session 2b writes; Session 3 extends with `customer-requested`, `business-cancelled`, `admin-manual`, `business-rejected-pending`), `initiated_by_user_id` (nullable — null for automatic), `failure_reason` (nullable), `created_at`, `updated_at`. One booking may have multiple refund rows (partial refunds accumulate; failed attempts stay for audit). The table is created here, not in Session 3 — the late-webhook refund path needs a real row to issue against.
- [ ] Minimal `App\Services\Payments\RefundService` with the single signature `refund(Booking $booking, ?int $amountCents = null, string $reason = ''): RefundResult`. In Session 2b, only the `cancelled-after-payment` reason path is exercised — full refund only (`amountCents = null`), no partial-refund UI, no multi-trigger dispatch. The service: (a) creates a `booking_refunds` row with `status = pending` AND a populated `uuid`; (b) calls `stripe.refunds.create([...], ['stripe_account' => $cachedAccountId, 'idempotency_key' => 'riservo_refund_'.$bookingRefund->uuid])` per locked decision #36's row-UUID rule; (c) updates the row on success / failure from the synchronous response; (d) applies the disconnected-account fallback per decision #36 (on Stripe permission error, mark the row `failed`, update `payment_status = refund_failed` on the booking, write `payment.refund_failed` Pending Action, dispatch admin email). Session 3 EXPANDS this service (customer cancel, admin manual, business cancel, manual-confirmation rejection paths, partial refunds, webhook-driven state transitions) without re-creating the table or re-defining the signature.
- [ ] `Booking::remainingRefundableCents()` (Session 2a's helper returning `paid_amount_cents` in isolation) now reads the real clamp — `paid_amount_cents - SUM(booking_refunds.amount_cents WHERE status IN (pending, succeeded))`. Session 3's partial-refund clamp in decision #37 lands on this already-correct helper.

### Failure webhook handling
- [ ] `checkout.session.expired`, `checkout.session.async_payment_failed`, and `payment_intent.payment_failed`: branch on the booking's `payment_mode_at_creation` per locked decision #14:
  - `online` → cancel the booking (status → cancelled, payment_status → `not_applicable` per the decision #28 terminal-value reading for a never-completed attempt), free the slot, no booking-confirmed notifications.
  - `customer_choice` → promote the booking to `confirmed` + `payment_status = unpaid`, clear `expires_at`, dispatch the standard booking-confirmed notifications (the customer made a real booking, just unpaid). Slot stays held.
  - `confirmation_mode = manual` intersects the above: if the Business is in manual confirmation AND the branch resolves to `confirmed`, the status stays `pending` per locked decision #29 — "confirmed" in the branch descriptions above reduces to "promoted to whatever the confirmation-mode path would have produced" (pending or confirmed).
- [ ] Outcome-level idempotency per locked decision #33 at the top of every handler.
- [ ] **Late-webhook refund path (locked decision #31)**: if `checkout.session.completed` OR `payment_intent.succeeded` arrives for a booking whose `status = cancelled` AND `payment_status != paid`, the handler marks `payment_status = paid`, persists the charge metadata, AND calls `RefundService::refund($booking, null, 'cancelled-after-payment')` synchronously — the minimal service introduced in this session's Data layer issues the refund end-to-end: `booking_refunds` row created, Stripe called with row-ID idempotency key, row updated with the outcome. The `payment.cancelled_after_payment` Pending Action is created AND an admin email is dispatched (both admin-scoped per decision #31's extended clause). No stubbing, no handoff to Session 3 — the refund attempt completes in 2b's lifetime. Session 3 expands `RefundService` with additional triggers (customer cancel, admin manual, business cancel, manual-confirmation rejection) and partial-refund support, but does NOT re-implement the late-webhook path; that path works end-to-end post-2b.

### Expiry reaper with defense-in-depth (locked decision #31)
- [ ] Scheduled job `bookings:expire-unpaid` running every 2 minutes with `withoutOverlapping()`. Base filter: `status = pending` + `payment_status = awaiting_payment` + `payment_mode_at_creation = 'online'` + `expires_at < now - 5 minutes` (the 5-minute grace buffer per locked decision #31). The `payment_mode_at_creation = 'online'` filter is mandatory per locked decision #13 — `customer_choice` bookings never expire via this path.
- [ ] **Pre-flight retrieve** before cancellation (locked decision #31): for each candidate booking, call `stripe.checkout.sessions.retrieve($session_id, ['stripe_account' => $connectedAccountId])`. If Stripe reports `status = complete` OR `payment_status = paid`, PROMOTE the booking inline via the Session 2a happy-path handler (idempotent per locked decision #33) and SKIP the cancel. If Stripe reports `status = expired` or `open` AND `payment_status != paid`, proceed with the cancel.
- [ ] Pre-flight failure (Stripe timeout / 5xx): the reaper leaves the booking alone for this tick and tries again on the next cadence. A hard Stripe 4xx on retrieve (session not found — extremely unusual) is logged and the booking is cancelled on the assumption that the session is gone.
- [ ] Cancellation here does NOT dispatch customer notifications — the customer knows they didn't complete payment.

### Return / failure UX (cancel_url)
- [ ] `cancel_url` lands on the public booking page with copy that branches on the booking's `payment_mode_at_creation` (looked up via `client_reference_id` + a lightweight read endpoint, OR encoded into the `cancel_url` itself as a query param at session-create time):
  - `online` → "Payment not completed — your slot is released" banner with the service pre-selected so retry is one click.
  - `customer_choice` → "Payment not completed — your booking is confirmed; pay at the appointment" banner with a link to the booking management page. The booking is promoted to `confirmed + unpaid` server-side by the webhook path — the cancel-URL hit is informational only; the actual state transition is webhook-driven for idempotency.
- [ ] 3DS / SCA is entirely Stripe-hosted (per locked decision #4); riservo does not handle authentication UI.
- [ ] If the Business's connected account has been disabled between booking creation and customer return (race), the cancel-URL page surfaces "This business is no longer accepting online payments — contact them directly"; the rest of the Session 5 polish pass finalises this copy.

### Admin dashboard touch-ups
- [ ] Booking detail page (dashboard): for online-payment bookings, show paid amount, payment method (Card / TWINT), Stripe charge ID as a deep-link to the Stripe dashboard (Connect account view). For `unpaid` bookings, show "Customer attempted online payment but did not complete — payment due at appointment" with the audit trail (attempted at, payment intent id if available). For `paid + pending` bookings (manual-confirmation businesses, locked decision #29), show "Payment received. Your confirmation or rejection will be communicated to the customer; rejection triggers an automatic full refund."
- [ ] Bookings list: new column / filter "Payment" with chips for `paid`, `awaiting_payment`, `unpaid`, `refunded`, `offline` (the last maps to `not_applicable`).
- [ ] Pending Action surface (from Session 1's infrastructure): `payment.cancelled_after_payment` rows render as a dashboard banner with the customer's contact details and a "Mark as resolved" CTA.

### Tests + ops
- [ ] Feature tests: `checkout.session.expired` for `payment_mode_at_creation = online` frees the slot; `checkout.session.expired` for `payment_mode_at_creation = customer_choice` promotes the booking to `confirmed + unpaid` and dispatches booking-confirmed notifications; `payment_intent.payment_failed` mirrors the same online-vs-customer_choice branching; `confirmation_mode = manual` intersects correctly with both failure branches.
- [ ] **`pending + unpaid` landing test (locked decision #29 variant)**: Business = `customer_choice` AND `confirmation_mode = manual` AND `checkout.session.expired` (or `payment_intent.payment_failed`) → the booking lands at `status = pending` + `payment_status = unpaid` (NOT `confirmed + unpaid`; the manual-confirmation gate keeps the status at pending). Slot stays held. The standard pending-booking notifications fire (admin is notified via `BookingReceivedNotification` context='new'; customer receives the "pending the business's confirmation" copy). Session 3 wires the admin-rejection branch for this state; here we only assert the landing state.
- [ ] Reaper tests: reaper cancels stale `online` bookings past the grace buffer; reaper leaves `customer_choice` bookings untouched; reaper's pre-flight retrieve promotes a paid-but-webhook-delayed booking inline and SKIPS the cancel; reaper with a Stripe timeout on retrieve leaves the booking alone this tick; reaper with a Stripe 404 on retrieve cancels (session gone).
- [ ] Late-webhook refund test (end-to-end in 2b, not stubbed): when `checkout.session.completed` arrives for a booking already cancelled by the reaper, the handler writes `payment_status = paid`, creates a `booking_refunds` row with `status = pending` and a populated `uuid`, calls `stripe.refunds.create` with `idempotency_key = 'riservo_refund_'.$row->uuid` (asserted, per locked decision #36), updates the row to `status = succeeded` on the Stripe response, and creates a `payment.cancelled_after_payment` Pending Action + dispatches the admin email. A disconnected-account variant of this test (Stripe returns permission error) asserts the row lands at `status = failed`, booking's `payment_status = refund_failed`, and a `payment.refund_failed` Pending Action + email fire instead.
- [ ] Idempotency test for the late-webhook path: re-delivering the same `checkout.session.completed` event ID hits the `stripe:connect:event:` cache and returns 200 without creating a second `booking_refunds` row (cache-layer dedup). A distinct scenario — an admin manually triggers the same refund intent within the same booking_refunds row's lifecycle — is covered in Session 3.
- [ ] Admin surface: payment panel renders for each payment_status value; bookings-list filter exposes the expected chip set; Pending Action banner surfaces `cancelled-after-payment` rows.
- [ ] FakeStripeClient extended for 2b at the SDK boundary: `mockRefundCreate($chargeId, $accountId, $amount, $currency, $idempotencyKey, $response)` — asserts presence of the `stripe_account` header AND asserts the idempotency key matches `'riservo_refund_'.{booking_refund_uuid}` per locked decision #36. Session 3 reuses this fake with additional reason-branch scenarios; Session 3 does not redefine it.
- [ ] Cross-tenant denial tests (locked decision #45): any admin dashboard action introduced in 2b (booking-detail payment panel, Pending Action resolution, bookings-list filter) is exercised by at least one test that asserts a request authenticated as an admin of Business A cannot read / mutate a Business B booking or Pending Action.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.

**Out of scope**: additional refund triggers beyond `cancelled-after-payment` (customer in-window cancel, admin manual, business cancel, manual-confirmation rejection — all Session 3), partial-refund UI + clamp (Session 3), payout surfacing (Session 4), Settings toggle lift (Session 5). A "Resend payment link" UX for `unpaid` bookings is explicitly deferred to BACKLOG — for MVP the customer simply pays at the appointment.

---

## Session 3 — Refunds (Customer Cancel, Admin Manual, Business Cancel) + Disputes

**Owner**: single agent. **Prerequisites**: Session 2b. **Unblocks**: Session 5 indirectly (the full payment lifecycle must work before we expose the mode toggle).

Wire every refund trigger and the dispute-awareness surface. Four distinct refund paths (customer cancel, admin manual, business cancel, manual-confirmation rejection) converge on one refund executor; three dispute events converge on a Pending Action.

### Refund executor
- [ ] **EXPAND `App\Services\Payments\RefundService`** (created in Session 2b with the minimal `cancelled-after-payment`-only path and the signature `refund(Booking $booking, ?int $amountCents = null, string $reason = ''): RefundResult`). Session 3 adds: (a) partial-refund support (`amountCents` non-null branch), (b) the full reason matrix — `customer-requested`, `business-cancelled`, `admin-manual`, `business-rejected-pending` alongside the existing `cancelled-after-payment` — each with its own notification / Pending-Action consequence, (c) webhook-driven state transitions from `charge.refunded` / `charge.refund.updated` / `refund.updated` (per Q6). The table, the signature, the row-ID-as-idempotency-key rule (locked decision #36), and the disconnected-account fallback already exist from Session 2b; Session 3 does NOT redefine them.
- [ ] **Amount basis (locked decision #37)**: the refund amount is always derived from `bookings.paid_amount_cents` (with a partial-refund clamp of `paid_amount_cents - SUM(booking_refunds.amount_cents WHERE status IN (pending, succeeded))`). `Service.price` is NEVER read by the refund path. A server-side assertion at the top of `RefundService::refund` rejects any amount exceeding the clamp with a 422. Session 2b's `Booking::remainingRefundableCents()` already computes the clamp correctly; Session 3 wires the UI and the controller validation on top.
- [ ] **Currency preservation (locked decision #42)**: the Stripe refund call passes the booking row's captured `currency`, not the connected account's current default. The partial-refund UI displays amounts in the booking's captured currency and formats via the existing Money helper.
- [ ] **Disconnected-account fallback (locked decision #36)** already exists from Session 2b. Session 3 ensures every new reason path (customer cancel, business cancel, admin manual, manual-confirmation rejection) surfaces the fallback gracefully in its own UI branch — same Pending Action type (`payment.refund_failed`), same admin email, same `refund_failed` `payment_status`.
- [ ] The `booking_refunds` table already exists from Session 2b (`id`, `booking_id`, `stripe_refund_id`, `amount_cents`, `currency`, `status`, `reason`, `initiated_by_user_id`, `failure_reason`, timestamps). Session 3 adds any column the expanded triggers require — none anticipated. The `reason` column's value space grows from `{cancelled-after-payment}` (Session 2b) to include `customer-requested`, `business-cancelled`, `admin-manual`, `business-rejected-pending`.

### Customer-side cancel (in-window → automatic full refund)
- [ ] `CustomerBookingController::cancel` (or equivalent public cancel endpoint): after the existing cancellation-window check (D-016) passes, if the booking has `payment_status = paid` OR `partially_refunded`, dispatch `RefundService::refund($booking)` with reason `customer-requested`. A booking with `payment_status = unpaid` (customer_choice fallback) cancels as today with no refund attempt — there is nothing to refund.
- [ ] The customer-facing success page reflects "Refund initiated — you'll receive it in your original payment method within 5–10 business days" (copy is Stripe's stock timing). On disconnected-account failure (locked decision #36), the copy instead reads "Your booking is cancelled. Because the business's payment setup has changed, the refund cannot be issued automatically — they will contact you to arrange it."
- [ ] Cancellation-window-outside still cancels the booking (consistent with existing behaviour, D-016) but issues no refund; the customer sees "Refund not automatic — contact the business".

### Admin dashboard — manual refund
- [ ] Booking detail page gains a "Refund" button visible only to admins (per locked decision #19) when `payment_status IN (paid, partially_refunded)` AND `remainingRefundableCents() > 0`. The button remains visible when the connected account is disconnected — the refund attempt is tried, and the disconnected-account fallback path is surfaced if it fails.
- [ ] Clicking opens a dialog: preset "Full refund" radio; alternative "Partial refund" with an amount input (bounded client-side by `remainingRefundableCents()`, enforced server-side per locked decision #37); free-form reason textarea.
- [ ] Submits to `Dashboard\BookingRefundController::store` which calls `RefundService::refund` with `initiated_by_user_id = auth()->id()` and reason `admin-manual`.
- [ ] Staff users see the Refund button as disabled with a "Admin-only" tooltip.

### Business-side cancel (always automatic full refund)
- [ ] Existing admin/staff cancel path (`BookingController::cancel` or wherever live): after the cancel succeeds, if `payment_status = paid`, dispatch `RefundService::refund($booking)` with reason `business-cancelled` (full amount, non-configurable per locked decision #17).
- [ ] Customer-facing cancel-notification email is extended to say "A full refund has been issued" when this path fires; disconnected-account fallback is surfaced in the email copy when the refund can't go through automatically.

### Manual-confirmation rejection → automatic full refund (locked decision #29)
- [ ] The existing admin "Reject" action on a `pending + paid` booking transitions the booking to `status = cancelled` AND dispatches `RefundService::refund($booking, null, 'business-rejected-pending')` in the same transaction boundary as the rejection. The customer-facing cancellation notification template branches on `payment_status`: for `paid`, the body includes "Because your booking could not be accepted, a full refund has been issued." The refund-clause paragraph is rendered conditionally, not as static copy.
- [ ] If the booking is `pending + awaiting_payment` (customer has not completed Checkout yet) and the admin rejects, no refund is due — the slot is cancelled, and the customer's next Checkout attempt (if any) will fail to find the session as `open` and fall through the usual failure path.
- [ ] **`pending + unpaid` rejection (locked decision #29 variant)**: the admin-rejection handler detects `payment_status = unpaid` on a `pending` booking (which arises only when Business = `customer_choice` AND `confirmation_mode = manual` AND the Checkout attempt failed — lands via Session 2b's `pending + unpaid` path) and cancels WITHOUT calling `RefundService::refund` — there is no charge to refund. The customer-facing cancellation template's refund clause is suppressed (same branch-on-`payment_status` logic as the `paid` → `paid` copy above; the `unpaid` branch renders a plain cancellation with no mention of money). Any template that unconditionally includes refund language is a regression.

### Cancelled-after-payment refund (already live in Session 2b)
- [ ] Session 3 reuses the already-live 2b refund path; the late-webhook → automatic full-refund flow is already covered in 2b. This section extends Session 2b's `RefundService` with the additional reason branches (customer in-window cancel, admin manual, business-side cancel, manual-confirmation rejection) and the partial-refund clamp per decision #37. The accompanying `payment.cancelled_after_payment` Pending Action stays in place as an admin-facing explanation even after the refund succeeds, so the admin can reach out to the customer to explain.

### Dispute webhook handling (locked decision #35)
- [ ] Connect webhook endpoint handles `charge.dispute.created`, `charge.dispute.updated`, `charge.dispute.closed`. On `created`: write a Pending Action of type `payment.dispute_opened` with a deep-link to the dispute in the Stripe Express dashboard; email the Business admins with the dispute reason verbatim and the same deep-link; do NOT change the booking's `status` or `payment_status` (per locked decision #25, payment_status is informative). On `updated`: refresh the Pending Action's detail (new evidence deadline, status transitions within Stripe). On `closed`: resolve the Pending Action with a "Dispute won" or "Dispute lost — funds returned to customer" summary, and email the admins with the outcome.
- [ ] Outcome-level idempotency per locked decision #33: handlers check the Pending Action's current state before writing, so replays don't spam admins with duplicate emails.

### Webhook handling — refunds
- [ ] Connect webhook endpoint subscribes to `charge.refunded`, `charge.refund.updated`, AND `refund.updated`. Stripe emits both `charge.refund.updated` and `refund.updated` across API-version boundaries — subscribing to the full set is belt-and-braces against Stripe dashboard test-endpoint config drift. At Session 3 plan time, verify the live API version the project's Stripe account targets (`config('cashier.stripe.api_version')` or the SDK default as of Session 3's open) to understand which events are primary vs deprecated for the current account. The D-092 cache-key dedup helper on `stripe:connect:event:{id}` handles the "same semantic event delivered under two names" case for free, because Stripe event IDs are unique across names — a refund state transition delivered as both `charge.refund.updated` and `refund.updated` dedupes on the first processed event. Handlers update the matching `booking_refunds` row's status + `failure_reason`; update the parent booking's `payment_status` to `refunded` (if total refunded = `paid_amount_cents` per locked decision #37) or `partially_refunded` or `refund_failed` accordingly.
- [ ] Refund-failed case: a dashboard banner on the booking detail page surfaces the failure + Stripe's failure reason verbatim, plus a Pending Action of type `payment.refund_failed`. Insufficient connected-account balance (locked decision #18) is one of the Stripe-handled causes — we do NOT block the refund attempt upfront.

### UI
- [ ] Booking detail: a "Payment & refunds" panel lists the original charge + every `booking_refunds` row (date, amount, status, initiator, reason). A "Dispute" section appears when a dispute Pending Action exists, deep-linking to Stripe.
- [ ] Public management page (customer): when a refund is in flight or complete, show a compact status line under the cancel button. Disconnected-account failure shows "The business will contact you to arrange your refund."

### Tests + ops
- [ ] Feature tests: in-window customer cancel issues automatic refund; out-of-window customer cancel issues no refund; admin manual full refund succeeds; admin partial refund bounded by `remainingRefundableCents()` server-side; staff cannot reach the refund endpoint (403); business cancel issues automatic refund; manual-confirmation rejection on a `pending + paid` booking issues a full refund AND the customer-facing cancellation notification includes the "a full refund has been issued" copy; manual-confirmation rejection on a `pending + unpaid` booking (customer_choice + manual-confirmation + failed Checkout, per locked decision #29's variant clause) cancels WITHOUT dispatching a refund AND the customer-facing cancellation notification omits the refund clause entirely (template branches on `payment_status != paid`); manual-confirmation rejection on a `pending + awaiting_payment` booking issues NO refund (Checkout never completed); `charge.refunded` webhook promotes status; `charge.refund.updated` with failure surfaces `refund_failed` + Pending Action; refunding an already-fully-refunded booking is rejected with a friendly error; idempotency replay (same admin double-click) produces exactly one refund; refund amount uses `paid_amount_cents` not `Service.price` after a price change (locked decision #37); refund currency preserves the booking's captured currency not the account's current default (locked decision #42); disconnected-account refund writes `refund_failed` + Pending Action and does NOT crash; late-webhook refund path from Session 2b completes end-to-end.
- [ ] Dispute tests: `charge.dispute.created` writes a Pending Action and emails admins; `charge.dispute.updated` refreshes the Pending Action without duplicate emails; `charge.dispute.closed` resolves the Pending Action with the right outcome; booking status is unchanged across all three.
- [ ] Cross-tenant denial tests (locked decision #45): admin of Business A cannot refund a Business B booking (404 via tenant-scoped lookup in `BookingRefundController`); admin of Business A cannot resolve a Business B Pending Action of any payment type; admin of Business A cannot view a Business B booking's Payment & refunds panel. One test per controller boundary introduced in this session.
- [ ] FakeStripeClient: reuse Session 2b's `mockRefundCreate` (already asserts `stripe_account` header + `idempotency_key` shape). Session 3 adds a `mockDisputeEvent` helper that constructs canonical `charge.dispute.*` event payloads for webhook tests, plus extra scenarios on `mockRefundCreate` covering the new reason branches.
- [ ] Email copy: customer-facing refund-issued email copy exists and is covered by a rendering test; admin-facing dispute-opened email copy is covered by a rendering test.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.

**Out of scope**: partial-refund presets, refund-on-no-show automatic policy (we default to no automatic refund on no-show — admin manual only), evidence-upload UI for disputes (that lives in Stripe).

---

## Session 4 — Payout Surface + Connected Account Health

**Owner**: single agent. **Prerequisites**: Session 3. **Unblocks**: Session 5.

Give the Business visibility into their money after it leaves the customer. Read-only, Stripe-sourced, no riservo-side payout orchestration.

### Data layer
- [ ] No new tables. Payout data is fetched live from Stripe on-demand and cached briefly in Laravel's cache (TTL decided at plan time, likely 60s). Persisting payout history locally is out of scope — Stripe is the source of truth.

### Controller
- [ ] `Dashboard\PayoutsController@index`: fetches the connected account's `balance`, `payouts.list` (last 10), and `payout_schedule` via Stripe API; returns an Inertia page. Admin-only.
- [ ] Graceful degradation when the connected account is not verified: the page shows the same onboarding CTA as Session 1's banner rather than crashing.

### UI
- [ ] `Dashboard/payouts.tsx`: four cards — Available balance (amount in default currency), Pending balance, Next payout ETA + amount, Recent payouts table (date, amount, status, arrival date).
- [ ] Prominent "Manage payouts in Stripe" button: generates an Express dashboard login link via Stripe API (`accounts.createLoginLink`) and opens it in a new tab. This is the single riservo-side action on payouts (per locked decision #22).
- [ ] Connected-account health strip at the top: charges-enabled / payouts-enabled / requirements-due, each a colour-coded chip with a tooltip for the underlying Stripe reason string.

### Error states
- [ ] Stripe API timeout / error: render cached data with a "Couldn't refresh — showing last known state" banner; never crash the page.
- [ ] Connected account disabled by Stripe: the whole page degrades to the `disabled_reason` banner + "Contact Stripe support" CTA.
- [ ] Stripe Tax not configured on the connected account: show the persistent warning banner per locked decision #11 — "Stripe Tax not yet configured — your customers' receipts will not show VAT. [Configure in Stripe →]".
- [ ] Non-CH connected account (per locked decision #43): `! in_array($account->country, config('payments.supported_countries'))` — show an info banner "Online payments in MVP support CH-located businesses only". The page still renders balance / payout info (it's read-only and harmless) but Session 5's Settings gate will prevent the Business from switching `payment_mode` to `online` or `customer_choice`. No hardcoded `'CH'` literal in the component — read the supported set from the Inertia prop that carries it, or from a short client-side helper.

### Navigation
- [ ] New nav item "Payouts" under the dashboard, admin-only, visible only when a connected account row exists. (Session 5 adjusts conditional visibility once the feature is broadly GA.)

### Tests + ops
- [ ] Feature tests: admin sees the page with faked Stripe responses; staff cannot reach the page (403); Stripe API error renders the degradation banner; Express dashboard link generation calls the right Stripe API; unverified account shows the onboarding CTA; Stripe-Tax-not-configured banner shows when `tax_settings.status !== 'active'`; non-CH account banner shows when `! in_array($country, config('payments.supported_countries'))` (locked decision #43's config-driven check); **cross-tenant denial (locked decision #45)** — admin of Business A cannot view Business B's payouts page (404 via tenant-scoped lookup).
- [ ] FakeStripeClient extended (connected-account-level category, header asserted present): `mockBalanceRetrieve($accountId, $response)`, `mockPayoutsList($accountId, $params, $response)`, `mockTaxSettingsRetrieve($accountId, $response)`. The Express-dashboard-login-link call is platform-level (uses `mockLoginLinkCreate` from Session 1, header asserted absent).
- [ ] Accessibility pass on the payouts page: status chips carry text + icon, not just colour; login-link button has descriptive aria-label.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.

**Out of scope**: payout schedule change, manual payout initiation, dispute dashboard (dispute awareness ships in Session 3), riservo-side payout history persistence.

---

## Session 5 — `payment_mode` Toggle Activation + UI Polish

**Owner**: single agent. **Prerequisites**: Sessions 1, 2a, 2b, 3, 4. **Unblocks**: nothing — this session closes the roadmap.

Lift the hide-the-options ban. Expose `online` and `customer_choice` in Settings → Booking. Walk through every touched surface and polish copy, empty states, error states, and edge cases discovered in earlier sessions.

### Settings → Booking toggle
- [ ] Settings → Booking re-exposes the full `payment_mode` select with three options: `offline`, `online`, `customer_choice`.
- [ ] Selecting `online` or `customer_choice` is gated on THREE conditions (all must be true):
  1. `Business.canAcceptOnlinePayments() === true` (verified connected account).
  2. `in_array($connectedAccount->country, config('payments.supported_countries'))` per locked decision #43 — non-supported countries see the options disabled with a "Online payments in MVP support CH-located businesses only" tooltip. No hardcoded `'CH'` literal at this call site.
  3. No other hard policy-level disable (reserved for future gates).
- [ ] When any condition fails, the options render disabled with the appropriate tooltip (priority order: not-connected → non-CH → other). Saving the setting with any condition false is rejected server-side with a 422 — the client-side disable is a convenience, not the enforcement (consistent with the D-068 gating pattern).
- [ ] Copy for each option: `offline` = "Customers pay on-site"; `online` = "Customers pay when booking"; `customer_choice` = "Customers choose at checkout". When `confirmation_mode = manual` AND `payment_mode ∈ {online, customer_choice}`, an inline hint reads "Customers will be charged at booking; if you reject a booking, they'll receive an automatic full refund" (per locked decision #29).

### UI polish across the feature
- [ ] Public booking flow: finalise copy around the payment step ("Secured by Stripe" microcopy, TWINT logo shown when available, clear "Your card will be charged CHF {amount}" confirmation line).
- [ ] Error banner on the public booking page when the Business's connected account becomes disabled mid-session (race): "This business is no longer accepting online payments right now — try again later or contact them directly."
- [ ] Empty-state copy on the Payouts page, the Connected Account page, and the booking detail refund panel — ensure all three have a consistent voice.
- [ ] Dashboard-wide banner consolidation: one banner system, not three competing ones (Session 1's onboarding banner, Session 3's refund-failed banner, Session 5 unifies the styling / stacking).
- [ ] Internationalisation pass: every string introduced in Sessions 1–4 goes through `__()` (per D-008); no retrofitting at end.

### Audit and cleanup
- [ ] Audit every place in the codebase that branches on `Business.payment_mode`; confirm no code path was left reading the `online` / `customer_choice` values as if they didn't exist. Add explicit `match ($business->payment_mode) { ... }` handling wherever branching was previously `$business->payment_mode === 'offline' ? A : B`.
- [ ] Audit the slug reserved list — no new reservation needed (`/payments` is not a public route), but confirm.
- [ ] Confirm the bookings list filter and the Pending Actions list (Calendar integration) both behave sanely for online bookings.

### Tests + ops
- [ ] Feature tests: admin can select `online` when connected AND country is in `config('payments.supported_countries')`; admin cannot select `online` when not connected; admin cannot select `online` when connected but country is not in the supported set (locked decision #43); a test that flips the supported-countries config to `['CH', 'DE']` in-test and asserts a DE account can then select `online` (proves seams are open and nothing is hardcoded); server-side 422 enforces both rules; `customer_choice` end-to-end (customer picks online → Checkout; customer picks offline → no Checkout); race test where connected account is disabled between page load and form submit; `confirmation_mode = manual` intersect test where admin rejects a paid pending booking → automatic refund fires (locked decision #29).
- [ ] Visual regression pass on every dashboard page that grew a banner or panel.
- [ ] Pint clean, full Pest suite green, `npm run build` clean.
- [ ] `docs/HANDOFF.md` updated; the roadmap is closed; decisions D-096.. are recorded in `docs/decisions/DECISIONS-PAYMENTS.md` (the new topical file opened in Session 1).

---

## Cleanup tasks (after this roadmap is approved)

These are housekeeping moves performed before Session 1 starts. Listed here so they are not forgotten — they are not part of the sessions themselves.

- [ ] Update `docs/SPEC.md` §12: remove the "Online payments / Stripe processing" row from the Out-of-Scope table (it has shipped with this roadmap). The separate "Social login (Google, Apple via Socialite)" row is untouched by this roadmap — social-login UX is unrelated to Stripe Connect (D-080 uses Socialite v5 for Google Calendar OAuth, not for customer social login). Leave it as v2.
- [ ] Update `docs/SPEC.md` §7.6 "Payment mode" line: remove the parenthetical "online payment requires Stripe integration (v2)" and replace with a sentence describing the now-live online / customer_choice flow and how to enable it (Connected Account onboarding).
- [ ] Update `docs/SPEC.md` §2 Business Model: keep "No commission on individual bookings"; add a sentence clarifying that online customer payments go 100% to the professional via Stripe Connect.
- [ ] Update `docs/BACKLOG.md`: remove or mark resolved any pre-existing Stripe Connect / online-payments entries superseded by this shipped work.
- [ ] Add a `docs/BACKLOG.md` entry: "Resend payment link for `unpaid` `customer_choice` bookings" — deferred from this roadmap's Session 2 out-of-scope. Captures the future ability to give the customer a second shot at prepaying online after a failed Checkout (today they default to paying at the appointment). Likely shape: a button on the customer's booking management page that mints a fresh Checkout session against the same booking row.
- [ ] Add a `docs/BACKLOG.md` entry: "Post-hoc online payment link for manual bookings" — deferred from locked decision #30. Captures the future ability to send a customer a payment link for a booking that was created manually (phone / walk-in). Out of scope for this roadmap because it requires a fresh Checkout-session-per-booking endpoint and out-of-band customer contact UX.
- [ ] Add a `docs/BACKLOG.md` entry: "Online payments for non-CH connected accounts" — deferred from locked decision #43. The seams are already open: `config('payments.supported_countries')` is the single switch, no hardcoded `'CH'` literal anywhere in application code, server-side country assertion is in place on every Checkout-creation path, and the `payment_method_types` branch in Session 2a's Checkout configuration already hedges `card`-only for non-CH entries in the supported set. The fast-follow roadmap extending to IT / DE / FR / AT / LI becomes: (1) flip `config('payments.supported_countries')` to the target list; (2) audit tax assumptions per decision #11 for each target country's VAT regime; (3) confirm Stripe Checkout supports the language matrix for each new country (decision #39); (4) re-verify the `card`-only-fallback UX against a real test account in each country. No application refactor expected.
- [ ] Add a `docs/BACKLOG.md` entry: "Payment conversion analytics" — capture funnel metrics for booking attempts → Checkout initiated → Checkout completed → refunded, so the product team can measure the online-payment feature's impact post-launch. Not scheduled in this roadmap to keep the shipping surface tight.
- [ ] Add a `docs/BACKLOG.md` entry: "Consolidated Connect UX for multi-business admins" — locked decision #22 keeps connected accounts per-Business for MVP, which is correct but creates UX friction for users admining multiple businesses. A post-MVP pass can introduce a single onboarding flow that walks through each Business's Stripe connection in sequence.
- [ ] Add a `docs/BACKLOG.md` entry: "In-dashboard dispute evidence flow" — locked decision #25 punts dispute resolution to Stripe's dashboard. A future session can surface evidence upload and deadline tracking inside riservo once dispute volume justifies the UX investment.
- [ ] `docs/decisions/DECISIONS-PAYMENTS.md` already exists as an empty topical file with the standard header comment; Session 1 starts populating it at `D-109`.
- [ ] Leave `docs/roadmaps/ROADMAP-E2E.md` and `docs/roadmaps/ROADMAP-GROUP-BOOKINGS.md` in place — independent of this work. When this roadmap closes, `docs/ROADMAP.md` is ready to be overwritten by whichever parked roadmap the developer promotes next (or a fresh one).

---

*This roadmap defines the WHAT. The HOW is decided per session by the implementing agent in `docs/PLAN.md` (one live file, overwritten each session). Each session leaves the full Pest suite green, Pint clean, and the Vite build green before close.*
